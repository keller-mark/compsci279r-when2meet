import React from 'react';
import ReactDOM from 'react-dom/client';
import Intro from './Intro'
import './index.css'

// Render the main App component to the <div id="root"/> element in ./index.html.
ReactDOM.createRoot(document.getElementById('root')).render(<Intro />);
